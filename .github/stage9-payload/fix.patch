diff --git a/contrib/space-compute/pkg/admission/source_test.go b/contrib/space-compute/pkg/admission/source_test.go
index 7345956..188afdc 100644
--- a/contrib/space-compute/pkg/admission/source_test.go
+++ b/contrib/space-compute/pkg/admission/source_test.go
@@ -22,7 +22,7 @@ func TestKubernetesTrustSourceUsesDerivedBindingAndSingleSecret(t *testing.T) {
 	domain := spacev1.DomainReference{Name: "leo-a", ClusterID: "leo-cluster", OrbitClass: spacev1.OrbitLEO}
 	ref := spacev1.SecretReference{Namespace: "kube-system", Name: "space-compute-reporter-public-keys", Key: "leo-a"}
 	binding := &spacev1.SpaceDomainReporterBinding{
-		TypeMeta:   metav1.TypeMeta{APIVersion: spacev1.SchemeGroupVersion.String(), Kind: "SpaceDomainReporterBinding"},
+		TypeMeta:   metav1.TypeMeta{APIVersion: spacev1.CanonicalAPIVersion, Kind: "SpaceDomainReporterBinding"},
 		ObjectMeta: metav1.ObjectMeta{Name: spacev1.ReporterBindingName(principal)},
 		Spec:       spacev1.SpaceDomainReporterBindingSpec{ReporterPrincipal: principal, Domain: domain, AllowedKinds: []string{"SpaceDomainResourceSummary"}, PublicKeyRef: ref},
 	}
diff --git a/contrib/space-compute/pkg/migration/migrator.go b/contrib/space-compute/pkg/migration/migrator.go
index 31d0cee..515a634 100644
--- a/contrib/space-compute/pkg/migration/migrator.go
+++ b/contrib/space-compute/pkg/migration/migrator.go
@@ -82,7 +82,6 @@ func (m *Migrator) migrateResource(ctx context.Context, resource Resource, targe
 	gvr := schema.GroupVersionResource{Group: spacev1.GroupName, Version: targetVersion, Resource: resource.Plural}
 	listClient := m.Dynamic.Resource(gvr)
 	var list *unstructured.UnstructuredList
-	var err error
 	if resource.Namespaced {
 		list, err = listClient.Namespace(metav1.NamespaceAll).List(ctx, metav1.ListOptions{})
 	} else {
diff --git a/contrib/space-compute/pkg/planner/planner.go b/contrib/space-compute/pkg/planner/planner.go
index 2959e4d..159e503 100644
--- a/contrib/space-compute/pkg/planner/planner.go
+++ b/contrib/space-compute/pkg/planner/planner.go
@@ -471,7 +471,7 @@ func statePolicyHardConstraintExplanation(policy spacev1.StatePolicy) spacev1.Co
 	case spacev1.PolicyBestEffort:
 		message += "; best-effort may rank degraded soft telemetry but cannot violate declared hard limits"
 	}
-	return accept("state_policy_hard_constraints", "state policy hard-constraint semantics", string(policy), "non-relaxable declared constraints", message)
+	return spacev1.ConstraintExplanation{Code: "state_policy_hard_constraints", Constraint: "state policy hard-constraint semantics", Observed: string(policy), Required: "non-relaxable declared constraints", Message: message}
 }
 
 func physicalDeviceConstraints(allocations []capacityAllocation) []spacev1.PhysicalDeviceConstraint {
