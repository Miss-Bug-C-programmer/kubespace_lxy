diff --git a/pkg/scheduler/plugins/gpustability/snapshot_store.go b/pkg/scheduler/plugins/gpustability/snapshot_store.go
index 017d17a..e53c1d4 100644
--- a/pkg/scheduler/plugins/gpustability/snapshot_store.go
+++ b/pkg/scheduler/plugins/gpustability/snapshot_store.go
@@ -383,3 +383,19 @@ func cloneDeviceFieldSet(in map[deviceMetricField]struct{}) map[deviceMetricFiel
 	}
 	return out
 }
+
+func resourceContextsEqual(left, right nodeResourceContext) bool {
+	return resourceMapsEqual(left.Allocatable, right.Allocatable) && resourceMapsEqual(left.Requested, right.Requested)
+}
+
+func resourceMapsEqual(left, right map[v1.ResourceName]int64) bool {
+	if len(left) != len(right) {
+		return false
+	}
+	for name, value := range left {
+		if right[name] != value {
+			return false
+		}
+	}
+	return true
+}
