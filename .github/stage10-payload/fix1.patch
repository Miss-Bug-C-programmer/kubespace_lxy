diff --git a/contrib/space-compute/pkg/planner/phase10_test.go b/contrib/space-compute/pkg/planner/phase10_test.go
index f70ada0..58b1dfa 100644
--- a/contrib/space-compute/pkg/planner/phase10_test.go
+++ b/contrib/space-compute/pkg/planner/phase10_test.go
@@ -7,6 +7,7 @@ import (
 	"encoding/json"
 	"sort"
 	"testing"
+	"time"
 
 	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
diff --git a/pkg/scheduler/plugins/gpustability/phase10_test.go b/pkg/scheduler/plugins/gpustability/phase10_test.go
index 6cbe97b..b016c65 100644
--- a/pkg/scheduler/plugins/gpustability/phase10_test.go
+++ b/pkg/scheduler/plugins/gpustability/phase10_test.go
@@ -8,6 +8,8 @@ import (
 	"sync/atomic"
 	"testing"
 	"time"
+
+	"k8s.io/apimachinery/pkg/types"
 )
 
 func TestPhase10DeadlineHeapSelectsOnlyDueTarget(t *testing.T) {
@@ -24,7 +26,7 @@ func TestPhase10DeadlineHeapSelectsOnlyDueTarget(t *testing.T) {
 	now := time.Now()
 	var selected scrapeTarget
 	for i := 0; i < 1000; i++ {
-		node := phase2Node(t, "http://127.0.0.1:32021", "deadline-"+formatIndex(i), "uid-"+formatIndex(i), "iluvatar.com/gpu")
+		node := phase2Node(t, "http://127.0.0.1:32021", "deadline-"+formatIndex(i), types.UID("uid-"+formatIndex(i)), "iluvatar.com/gpu")
 		node.Annotations[AnnotationExporterPath] = "/metrics/" + node.Name
 		target, _, err := collector.ensureTarget(node)
 		if err != nil {
