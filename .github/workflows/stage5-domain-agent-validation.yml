name: stage5-domain-agent-validation
on:
  push:
    branches: [main]
    paths: [.github/stage5-trigger]
permissions: {contents: write, statuses: write}
jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with: {ref: "${{ github.sha }}", fetch-depth: 0}
      - name: Publish run identity
        env: {GH_TOKEN: "${{ github.token }}"}
        run: gh api --method POST "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" -f state=pending -f context="sandbox/stage5-domain-agent-${GITHUB_RUN_ID}" -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" -f description="Stage 5 domain-agent validation is running"
      - uses: actions/setup-go@v7
        with: {go-version-file: go.mod, cache: true}
      - name: Apply candidate and run focused compile
        shell: bash
        run: |
          set +e
          (
            set -euo pipefail
            python3 -m py_compile .github/stage5-api.py .github/stage5-runtime.py .github/stage5-fix.py .github/stage5-fix2.py .github/stage5-tests-direct.py .github/stage5-agent-core.py .github/stage5-fix3.py .github/stage5-core-tests.py .github/stage5-fix4.py .github/stage5-agent-lib.py .github/stage5-fix5.py .github/stage5-fix6.py
            python3 .github/stage5-api.py
            python3 .github/stage5-runtime.py
            python3 .github/stage5-fix.py
            python3 .github/stage5-fix2.py
            python3 .github/stage5-tests-direct.py
            gofmt -w contrib/space-compute/pkg/apis/v1alpha1/*.go contrib/space-compute/pkg/admission/*.go contrib/space-compute/pkg/planner/*.go contrib/space-compute/pkg/workload/*.go contrib/space-compute/pkg/kube/*.go contrib/space-compute/pkg/transport/*.go contrib/space-compute/pkg/execution/*.go
            python3 .github/stage5-agent-core.py
            python3 .github/stage5-fix3.py
            python3 .github/stage5-core-tests.py
            python3 .github/stage5-fix4.py
            gofmt -w contrib/space-compute/pkg/apis/v1alpha1/*.go contrib/space-compute/pkg/admission/*.go contrib/space-compute/pkg/planner/*.go contrib/space-compute/pkg/workload/*.go contrib/space-compute/pkg/kube/*.go contrib/space-compute/pkg/transport/*.go contrib/space-compute/pkg/execution/*.go
            python3 .github/stage5-agent-lib.py
            gofmt -w contrib/space-compute/pkg/transport/*.go
            python3 .github/stage5-fix5.py
            python3 .github/stage5-fix6.py
            gofmt -w contrib/space-compute/pkg/transport/*.go
            git diff --check
            go test ./contrib/space-compute/pkg/apis/v1alpha1 ./contrib/space-compute/pkg/execution ./contrib/space-compute/pkg/transport ./contrib/space-compute/pkg/admission ./contrib/space-compute/pkg/planner ./contrib/space-compute/pkg/workload ./contrib/space-compute/pkg/kube -count=1 -timeout=180s
            go test ./cmd/space-compute-mission-planner -count=1 -timeout=180s
          ) > /tmp/stage5-focused.log 2>&1
          status=$?
          cat /tmp/stage5-focused.log
          if [ "$status" -ne 0 ]; then
            git fetch origin main
            if [ "$(git rev-parse origin/main)" = "$GITHUB_SHA" ]; then
              { echo "run_id=$GITHUB_RUN_ID"; echo "base_sha=$GITHUB_SHA"; echo '--- focused output ---'; tail -n 800 /tmp/stage5-focused.log; } > .github/stage5-failure.txt
              git add .github/stage5-failure.txt
              git config user.name space-compute-stage5-bot
              git config user.email space-compute-stage5-bot@users.noreply.github.com
              git commit -m 'chore: capture phase5 focused failure' && git push origin HEAD:main
            fi
            exit "$status"
          fi
      - name: Publish success
        if: success()
        env: {GH_TOKEN: "${{ github.token }}"}
        run: gh api --method POST "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" -f state=success -f context="sandbox/stage5-domain-agent-${GITHUB_RUN_ID}" -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" -f description="Stage 5 focused candidate passed"
      - name: Publish failure
        if: failure()
        env: {GH_TOKEN: "${{ github.token }}"}
        run: gh api --method POST "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" -f state=failure -f context="sandbox/stage5-domain-agent-${GITHUB_RUN_ID}" -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" -f description="Stage 5 focused candidate failed"
